---

interface Props {
    headline: string,
    className?: string
}

const { headline, className } = Astro.props
---

<header class={`flex justify-between items-center ${className ?? " "}`}>
    <h5 class="md:text-subtitle font-semibold capitalize">{headline}</h5>
    <slot />
</header>